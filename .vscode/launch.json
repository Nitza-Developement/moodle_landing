{
  "version": "0.2.0",
  "configurations": [

    {
      "name": "Depurar Astro Dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm run dev",  // o "pnpm dev", "yarn dev", etc.
      "cwd": "${workspaceFolder}"
    }
  ]
}
